{
    "version": "0.2.0",
    "configurations": [
        {
            "name": ".NET Core Attach (AssetSquirrelAuthorize.WebApp)",
            "type": "coreclr",
            "request": "attach",
            "processId": "${command:pickProcess}"
        }
    ]
}
